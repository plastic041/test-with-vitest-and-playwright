## How to write vitest & playwright tests

vitest unit tests are colocated in `src/lib/*.spec.ts`.
playwright e2e tests are in `e2e/`.

Using vite, unocss, framer-motion, jotai, immer with vitest & playwright.